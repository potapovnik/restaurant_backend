package ru.relex.restaurant.db.JpaRepository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface WaiterOrdersRepository extends JpaRepository<WaiterOrdersRepository,Integer> {
}
